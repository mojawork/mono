export type UiInputComponentType = 'text' | 'number';

/*
const types = ['text', 'number'] as const;
export type UiInputComponentType = typeof types[number];
const isFruit = (x: any): x is UiInputComponentType => types.includes(x);

 */
